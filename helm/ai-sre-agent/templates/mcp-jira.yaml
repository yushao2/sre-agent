{{- if .Values.mcpServers.jira.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-sre-agent.fullname" . }}-mcp-jira
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-sre-agent.mcpLabels" (dict "context" . "name" "jira") | nindent 4 }}
spec:
  replicas: {{ .Values.mcpServers.jira.replicaCount }}
  selector:
    matchLabels:
      {{- include "ai-sre-agent.mcpSelectorLabels" (dict "context" . "name" "jira") | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
        {{- with .Values.mcpServers.jira.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "ai-sre-agent.mcpSelectorLabels" (dict "context" . "name" "jira") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "ai-sre-agent.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: mcp-jira
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          image: {{ include "ai-sre-agent.image" (dict "image" .Values.mcpServers.jira.image "Values" .Values) }}
          imagePullPolicy: {{ .Values.mcpServers.jira.image.pullPolicy }}
          {{- with .Values.mcpServers.jira.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: JIRA_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-sre-agent.secretName" . }}
                  key: JIRA_URL
            - name: JIRA_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-sre-agent.secretName" . }}
                  key: JIRA_USERNAME
            - name: JIRA_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-sre-agent.secretName" . }}
                  key: JIRA_API_TOKEN
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.mcpServers.jira.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.mcpServers.jira.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mcpServers.jira.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mcpServers.jira.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-sre-agent.fullname" . }}-mcp-jira
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-sre-agent.mcpLabels" (dict "context" . "name" "jira") | nindent 4 }}
spec:
  type: {{ .Values.mcpServers.jira.service.type }}
  ports:
    - port: {{ .Values.mcpServers.jira.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-sre-agent.mcpSelectorLabels" (dict "context" . "name" "jira") | nindent 4 }}
{{- end }}
